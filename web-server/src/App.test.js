import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Section emptied to avoid crash -- App component attempts to Fib with attempts to access React server (not running)
it('renders without crashing', () => {
});
